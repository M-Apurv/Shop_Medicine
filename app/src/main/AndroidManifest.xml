<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shopmedicine">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShopMedicine">
        <activity android:name=".Detail"></activity>
        <activity android:name=".AppDetail" />
        <activity android:name=".Order" />
        <activity android:name=".AllergiesActivity" />
        <activity android:name=".AllergiesOrderActivity" />
        <activity android:name=".HeadacheActivity" />
        <activity android:name=".HeadacheOrderActivity" />
        <activity android:name=".StomachacheActivity" />
        <activity android:name=".StomachacheOrderActivity" />
        <activity android:name=".ColdsFluActivity" />
        <activity android:name=".ColdsFluOrderActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>